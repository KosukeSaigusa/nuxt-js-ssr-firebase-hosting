{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix nuxtapp run build && rm -rf functions/nuxt && cp -r nuxtapp/.nuxt/ functions/nuxt/"
    ]
  },
  "hosting": {
    "predeploy": [
      "rm -rf public/* && mkdir -p public/_nuxt && cp -r nuxtapp/.nuxt/dist/client/ public/_nuxt && cp -a nuxtapp/static/. public/"
    ],
    "public": "public",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "function": "nuxtApp"
      }
    ]
  }
}
